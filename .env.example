# VinylFlow Configuration
# Copy this file to .env and update with your settings
# For Docker: cp .env.example .env && nano .env

# =============================================================================
# REQUIRED: Discogs API Settings
# =============================================================================
# Get your personal token from: https://www.discogs.com/settings/developers
# Without this token, metadata search and tagging will not work
DISCOGS_USER_TOKEN=your_token_here

# User agent for Discogs API requests (can leave as default)
DISCOGS_USER_AGENT=VinylFlow/1.0

# =============================================================================
# Output Settings
# =============================================================================
# Where processed FLAC files will be saved
# Docker: Use /app/output (mounted to ./output on host)
# Local:  Use ~/Music/VinylFlow or any preferred directory
DEFAULT_OUTPUT_DIR=/app/output

# =============================================================================
# Audio Processing Settings
# =============================================================================
# Silence threshold in dB (negative value, more negative = quieter)
# Adjust if tracks aren't splitting correctly:
#   -35 = More aggressive (splits on quieter passages)
#   -40 = Balanced (default)
#   -45 = More permissive (only splits on very quiet passages)
DEFAULT_SILENCE_THRESHOLD=-40

# Minimum silence duration in seconds to consider as track boundary
# Increase if you get too many splits, decrease if tracks merge together
DEFAULT_MIN_SILENCE_DURATION=1.0

# Minimum track length in seconds (filter out false positives)
# Segments shorter than this will be ignored
DEFAULT_MIN_TRACK_LENGTH=30

# FLAC compression level (0-8, higher = more compression but slower)
# 8 = Maximum compression (recommended, ~40% smaller)
# 5 = Balanced (faster encoding)
# 0 = No compression (fastest but larger files)
DEFAULT_FLAC_COMPRESSION=8

# =============================================================================
# Server Configuration (Docker only)
# =============================================================================
# Port for the web interface
# Change if 8000 is already in use
PORT=8000
